import { useState } from "react";

export const App = () => {
    return (
        <div className="h-screen flex justify-center items-center">
            <h1>Olá Mundo!</h1>
        </div>
    );
};
